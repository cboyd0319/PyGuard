name: CodeQL Security Analysis

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * 1'  # Weekly on Monday at 00:00 UTC
  workflow_dispatch:  # Allow manual trigger

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      with:
        fetch-depth: 1

    - name: Initialize CodeQL
      uses: github/codeql-action/init@9887d98ae49f1f598651b556d8c8f02f3ea065cb # v3.27.0
      with:
        languages: python
        queries: security-extended
        config: |
          paths-ignore:
            - tests/**
            - examples/**
            - benchmarks/**
            - docs/**

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@9887d98ae49f1f598651b556d8c8f02f3ea065cb # v3.27.0
      with:
        category: "/language:python"

    - name: Add security summary
      if: always()
      shell: bash
      run: |
        {
          echo "## 🔒 CodeQL Security Analysis Complete"
          echo ""
          echo "- **Language:** Python"
          echo "- **Queries:** security-extended"
          echo "- **Scope:** Production code (excluding tests/examples/benchmarks/docs)"
          echo ""
          echo "View results in the Security tab."
        } >> "${GITHUB_STEP_SUMMARY}"
